apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-reverseproxy-deployment
  labels:
    app: backend-reverseproxy-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend-reverseproxy
  template:
    metadata:
      labels:
        app: backend-reverseproxy
    spec:
      containers:
      - name: backend-reverseproxy-node
        image: "chungvt/reverseproxy:v1"
        imagePullPolicy: Always
        resources:
              requests:
                memory: "128Mi"
                cpu: "500m"
              limits:
                memory: "512Mi"
                cpu: "1000m"
        ports:
          - containerPort: 8080
          